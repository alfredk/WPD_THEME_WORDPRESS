(function($) {

	$( ".fa-search" ).click(function() {
  	$( "header .search-form" ).toggle('fast');
	});

	// $( ".search-field" ).toggle( showOrHide );

	// if ( showOrHide === true ) {
	//   $( ".search-field" ).show();
	// } else if ( showOrHide === false ) {
	//   $( ".search-field" ).hide();
	// }

})(jQuery); // Fully reference jQuery after this point.